- title 'New Show', true

- form_for(@show) do |f|
  = f.error_messages
    
  = render :partial => 'form', :object => f

  .submit_or_cancel
    = f.submit :create, :class => :submit
    %br
    = link_to :cancel, nil, :class => :cancel, :onclick => "parent.Shadowbox.close();"
