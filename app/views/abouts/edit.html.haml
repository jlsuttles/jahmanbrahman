- title "Edit About", true

- form_for(@about) do |f|
  = f.error_messages

  = render :partial => 'form', :object => f
    
  .submit_or_cancel
    = f.submit :save, :class => :submit
    %br
    = link_to :cancel, nil, :class => :cancel, :onclick => "parent.Shadowbox.close();"
