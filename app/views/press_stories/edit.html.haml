- @active_tab = :press
- title "Edit Press Story #{h @press_story.title}"

- form_for(@press_story) do |f|
  = f.error_messages
  = render :partial => 'form', :object => f
  = f.submit 'Save Changes', :class => 'submit'
  = link_to 'Cancel', press_stories_path, :class => 'cancel'
