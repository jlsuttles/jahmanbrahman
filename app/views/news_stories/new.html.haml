- @active_tab = :news
- title 'New News Story'

- form_for(@news_story) do |f|
  = f.error_messages
  = render :partial => 'form', :object => f
  = f.submit 'Create', :class => 'submit'
  = link_to 'Cancel', news_stories_path, :class => 'cancel'