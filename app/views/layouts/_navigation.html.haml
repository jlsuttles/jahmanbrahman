%ul#navigation
  %li= link_to 'Home',    root_path,    :class => "first item #{active_when(:home)}"
  %li= link_to 'About',   about_us_path,:class => "item #{active_when(:about)}"
  %li= link_to 'Music',   music_path,   :class => "item #{active_when(:music)}"
  %li= link_to 'Flyers',  flyers_path,  :class => "item #{active_when(:flyers)}"
  %li= render :partial => 'layouts/fb_like_no_faces'
.clear
